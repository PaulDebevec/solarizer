import { currentProfileReducer, userQuoteReducer, allUserQuotesReducer } from '../reducers/solarData'

describe('userProfile Reducers', () => {
  it('should return the initial state', () => {

  })
})